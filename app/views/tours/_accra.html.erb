<% if @tour.name == "Accra" %>
<div class="itinerary">
        <div class="itinerary-wrapper">
    
            <div class="tour-map-wrapper">
                <%= image_tag "accra-map", class: "tour-map" %>
            </div>
    
            <div class="itinerary-details">
                <% @tour.sites.each do |site| %>
                    <div class="itinerary-text">
                        <h1><%= site.id %></h1>
                        <h1><%= site.name %></h1>
                        <p style="width: 100%;"><%= site.description %></p>
                    </div>
                <% end %>
            </div>
        </div>
        
    </div>    
<% end %>
